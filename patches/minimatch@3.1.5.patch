diff --git a/minimatch.js b/minimatch.js
index 2e4a058a130dceebf83a7bd435f5d22b765ac836..269669441c15c1d699d8d144111b306ae04a70ea 100644
--- a/minimatch.js
+++ b/minimatch.js
@@ -7,7 +7,12 @@ var path = (function () { try { return require('path') } catch (e) {}}()) || {
 minimatch.sep = path.sep
 
 var GLOBSTAR = minimatch.GLOBSTAR = Minimatch.GLOBSTAR = {}
-var expand = require('brace-expansion')
+// PATCH: brace-expansion >=5.0.8 (the only release fixing GHSA-mh99-v99m-4gvg, a high-severity
+// unbounded-expansion DoS) exports a namespace object instead of a callable. Every
+// brace-expansion <= 5.0.7 is affected, so no in-major upgrade exists for the 1.x line
+// minimatch@3 asks for. Reading .expand off the namespace is what lets this package
+// consume the patched release; the function itself is signature-identical (verified).
+var expand = require('brace-expansion').expand || require('brace-expansion')
 
 var plTypes = {
   '!': { open: '(?:(?!(?:', close: '))[^/]*?)'},
