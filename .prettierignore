# oxfmt reads .prettierignore by default. These entries keep
# `pnpm run format:check` green across releases and agent runs.

# Generated by standard-version on each release — do not format-check.
CHANGELOG.md

# Claude Code tooling state + agent-written memory notes. Auto-generated
# Markdown in the agents' own style, not project source — must not gate the
# build's format check.
.claude/
